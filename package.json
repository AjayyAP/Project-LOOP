{
  "name": "project-loop",
  "version": "1.0.0",
  "private": true,
  "description": "Project LOOP MERN monorepo",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server"
  }
}
